<html lang='en'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Pending Orders</title>

    <style>
      *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box;
      font: 'Playfair Display', Georgia, serif; } body { background-color:
      #f2fafc; padding: 1rem; } .top-line { padding: 5px; background: #fb3c2d; }
      .company-name { font-size: 1rem; color: #44464a; padding: 1rem;
      text-align: center; } .heading { color: #44464a; font-size: 30px;
      text-align: center; font-weight: 500; } /* CARD */ .card { padding: 2rem;
      background-color: #fff; max-width: 650px; font-size: 14px; font-family:
      Nunito, Arial, Helvetica Neue, Helvetica, sans-serif; display: flex;
      flex-direction: column; justify-content: flex-start; gap: 1rem; margin:
      25px auto; border-radius: 5px; box-shadow: 0 0 10px rgba(45, 45, 45, 0.1);
      } .card__item :last-child { color: red; } /* TABLE */ .table-container {
      overflow: scroll; } .content-table { border-collapse: collapse; margin: 0
      auto; width: 100%; max-width: 650px; font-size: 0.6em; border-radius: 5px
      5px 0 0; overflow: hidden; overflow: scroll; box-shadow: 0 0 20px rgba(45,
      45, 45, 0.2); font-family: Nunito, Arial, Helvetica Neue, Helvetica,
      sans-serif; } .content-table thead tr { background-color: #fb3c2d; color:
      #ffffff; text-align: left; } .content-table th, .content-table td {
      padding: 12px 15px; font-size: 14px; } .content-table tbody tr {
      border-bottom: 1px solid #dddddd; } .content-table tbody
      tr:nth-of-type(even) { background-color: #f3f3f3; } .content-table tbody
      tr:last-of-type { border-bottom: 2px solid #fb3c2d; } .content-table tbody
      tr.active-row { color: #fb3c2d; } /* PRODUCT */ .product { display: flex;
      gap: 8px; align-items: center; min-width: 200px; } .product__image img {
      display: block; aspect-ratio: 1; max-width: 30px; border-radius: 5px; }
      footer { display: flex; flex-direction: column; gap: 1rem; align-items:
      center; text-align: center; justify-content: center; } .footer p {
      text-align: center; } .btn { border: none; outline: none; border: 2px
      solid #fb3c2d; padding: 0.8rem; border-radius: 5px; text-decoration: none;
      color: #fb3c2d; display: inline-block; }
    </style>
  </head>

  <body>
    <div class='top-line'></div>
    <div class='company-name'>Supreme Sales Agency</div>
    <h1 class='heading'>Orders With Pending Status</h1>

    <div class='card'>
      <div class='card__item'>
        <span>Total Orders Pending: </span>
        <span>{{totalOrdersPending}}</span>
      </div>

      <div class='card__item'>
        <span>Report Generation Date: </span>
        <span>{{reportGenerationDate}}</span>
      </div>
    </div>

    <section class='table-container'>
      <table class='content-table'>
        <thead>
          <tr>
            <th>Order ID</th>
            <th>Product</th>
            <th>Qty</th>
            <th>Customer</th>
            <th>Mobile</th>
            <th>E-mail</th>
          </tr>
        </thead>

        <tbody>
          {{#each orders}}
            <tr>
              <td>{{_id}}</td>
              <td>
                <div class='product'>
                  <div class='product__image'>
                    <img
                      src={{getProductImage products.product_images}}
                      alt=''
                    />
                  </div>
                  <div class='product__name'>{{products.product_name}}</div>
                </div>
              </td>
              <td>{{products.product_quantity}}</td>
              <td>{{customer_name}}</td>
              <td>{{customer_phone_number}}</td>
              <td>{{customer_email}}</td>
            </tr>
          {{/each}}

        </tbody>
      </table>
    </section>

    <footer class='card'>
      <p>Visit the dashboard to get more information about orders.</p>
      <div><a class='btn' href=''>Go To Dashboard</a></div>
    </footer>
  </body>

</html>
